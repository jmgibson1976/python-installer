# {{ project_name }} — application configuration
#
# Add config sections here. Access them via:
#   from {{ pkg_name }}.config import settings
#   print(settings.app.name)
#   print(settings["app"]["debug"])

[app]
name = "{{ project_name }}"
version = "{{ version }}"
debug = false
