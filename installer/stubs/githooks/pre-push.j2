#!/usr/bin/env bash
# Delegate to global Copilot hook if present, then exit with its status.
# Falls back silently if the hook is missing or not executable.
HOOK="$HOME/.copilot/hooks/pre-push"
[[ -x "$HOOK" ]] && exec "$HOOK" "$@"
exit 0
